# Sample Inputs

- Explain: --topic "vector embeddings" --level "beginner"

- Summarize: `--text 'The diverse "Python" ecosystem includes core implementations like the standard **CPython**, the performance-boosting **PyPy**, and the microcontroller-focused **MicroPython**; pre-packaged distributions like the data-science powerhouse **Anaconda**; the evolution from legacy **Python 2** to the current **Python 3** standard; and, for the nature enthusiasts, the roughly **40 species of snakes** that originally inspired the name.'

- Extract: --text "Memoization caches return values ..."

- Structured: --task "Launch internal AI assistant pilot"
